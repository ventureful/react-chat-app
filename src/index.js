import React from 'react';
import ReactDOM from 'react-dom';
import AppMain from './components/AppMain';

ReactDOM.render(<AppMain name={"Sean's initial component"}/>, document.getElementById('projectRoot'));